# Substringler 

"""
Using substring, we can read certain data from within a string expression by skipping over.
To do this with substring, we need to specify a starting point. Similarly, the ending index is optional.

"""

ornek_cümle = "Merhaba Ben Enes"

print(ornek_cümle[1]) # ==> e

print(ornek_cümle[12]) # ==> E

print(ornek_cümle[0:3]) # ==> Mer
